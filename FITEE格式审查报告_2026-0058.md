Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Now I have all the information needed. The `\doi` command in fitee.sty automatically prepends `https://doi.org/` (line 185: `\def\ZUS@label@doi{https://doi.org/}`), so the DOI is correct. The `\shortauthor` is commented out in fitee.sty and not used in the tex file — no issue.

Let me now compile the complete report with exact line numbers.

---

# FITEE 格式审查报告 — FITEE-2026-0058

---

## 一、首页信息（共 0 处）

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 标题大小写 | ✅ 通过 — 首词 `Power` 大写，其余非专有名词小写 |
| 2 | 作者姓大写 | ✅ 通过 — `FEI`, `YU`, `CHEN`, `YU` 均全大写，双字名无空格 |
| 3 | 关键词分号分隔 | ✅ 通过 — `Broadband linearization; digital predistortion; multimode filter; power amplifier` |
| 4 | DOI 格式 | ✅ 通过 — fitee.sty 自动补全 `https://doi.org/` 前缀 |
| 5 | `\shortauthor` | ✅ 通过 — 模板注释了该命令，tex 未使用 |

---

## 二、图表（共 18 处）

### Item 9 — 图/表标题句末有句号（6处）

| # | 行号 | 当前内容 | 建议 |
|---|------|---------|------|
| 1 | **152** | `\caption{Overview of the proposed \FPAFB. }` | 删除句末句号和空格 → `\caption{Overview of the proposed \FPAFB}` |
| 2 | **234** | `\caption{Simulated frequency variations under change of (a) $L_P$ and (b) $\theta$. }` | 删除句末句号和空格 |
| 3 | **254** | `\caption{Output matching circuit with feedback port based on asymmetrical coupler. }` | 删除句末句号和空格 |
| 4 | **272** | `\caption{Final configuration and dimensions of the proposed multifunctional FPA-FB for analog filtering. }` | 删除句末句号和空格 |
| 5 | **378** | `\caption{...characteristics. }` | 删除句末句号和空格 |
| 6 | **431** | `\caption{Overview of the experimental DPD test bench setup. }` | 删除句末句号和空格 |

### Item 10 — 子图说明首字母应小写（4处）

| # | 行号 | 当前 | 建议 |
|---|------|------|------|
| 7 | **103** | `(a) Traditional linearization...` | → `(a) traditional...` |
| 8 | **304** | `(a) Diagram of the proposed...` | → `(a) diagram...` |
| 9 | **378** | `(a) Fabricated... (b) Transmission...` | → `(a) fabricated... (b) transmission...` |
| 10 | **388** | `(a) Simulated and measured...` | → `(a) simulated...` |

### Item 32 — 子图说明间缺分号分隔（2处）

| # | 行号 | 当前 | 建议 |
|---|------|------|------|
| 11 | **234** | `(a) $L_P$ and (b) $\theta$` | → `(a) $L_P$; (b) $\theta$` |
| 12 | **378** | `(a) Fabricated... (b) Transmission...` | 在 `(b)` 前加 `; ` |

### Item 31 — 引用标签与编号间缺 `~`（3处）

| # | 行号 | 当前 | 建议 |
|---|------|------|------|
| 13 | **256** | `as in Fig.9` | → `as in Fig.~9` |
| 14 | **395** | `Table I provides` | → `Table~I provides` |
| 15 | **426** | `in Table II` | → `in Table~II` |

### 表格格式问题（3处）

| # | 行号 | 检查项 | 问题与建议 |
|---|------|--------|-----------|
| 16 | **457–465** | 表头不应加粗（规则 3.7） | Table II 表头全部使用 `\textbf{}`，应改为白体（去粗）：`Method`, `Coefficients`, `NMSE(dB)`, `ACLR(dBc)`, `OSR` |
| 17 | **401** | 通栏表格宽度 18 cm（规则 3.6） | `\begin{tabular*}{16.2cm}` → `\begin{tabular*}{18cm}` |
| 18 | **412** | 表注 `\footnotesize`（规则 3.5） | `\scriptsize` → `\footnotesize` |

---

## 三、公式（共 8 处）

### Item 12/4.1 — 多字母缩略/常量应正体（3类问题跨多行）

| # | 行号 | 问题 | 建议 |
|---|------|------|------|
| 19 | **263, 343** | `\pi`（圆周率应为正体） | → `\mathrm{\pi}` |
| 20 | **214, 339, 343** | `j`（虚数单位应为正体） | → `\mathrm{j}` |
| 21 | **339, 343** | `e`（自然常数应为正体） | → `\mathrm{e}` |

### Item 13/4.2 — 公式下标多字母缩略应正体（2处）

| # | 行号 | 问题 | 建议 |
|---|------|------|------|
| 22 | **214, 260, 263** | `Z_{opt}`, `R_{opt}`, `X_{opt}` — "opt" 为多字母缩写 | → `Z_{\mathrm{opt}}`, `R_{\mathrm{opt}}`, `X_{\mathrm{opt}}` |
| 23 | **349, 352** | `y_{\text{CF}}` — 下标 "CF" 应用 `\mathrm` | → `y_{\mathrm{CF}}` |

### Item 16 — 公式结尾缺标点（2处）

| # | 行号 | 公式 | 问题 | 建议 |
|---|------|------|------|------|
| 24 | **324** | Eq. (3) — GMP 模型 | 末尾无标点；后接 `where $x(n)$...`（从句） | 加 `,` |
| 25 | **356** | Eq. (5) — CF-DPD 模型 | 末尾无标点；后接 `Based on the analysis...`（新句） | 加 `.` |

### Item 22 — 矩阵/矢量粗斜体

| # | 说明 |
|---|------|
| — | **跳过**（规则要求人工审查），本文暂未发现明显违规的单字母向量/矩阵 |

---

## 四、参考文献（共 4 处）

### Item 29 — 文献列表排序错误（2处）

| # | 行号 | 问题 |
|---|------|------|
| 26 | **548–570** | **Chen SZ and Kang (2018, 2 作者)** 排在 Chen KL et al. (2011, 4+ 作者) 之后。同姓一作时，应按作者数从少到多排列：2 作者应在 4+ 作者**之前**。需将 `5g` (line 566) 移至 `FPA3` (line 548) 之前 |
| 27 | **669–678** | **Ruppel CCW (2017, 1 作者)** 排在 Ruppel CCW et al. (2002, 3+ 作者) 之后。同姓一作按作者数从少到多：1 作者应在 3+ 作者**之前**。需将 `saw2` (line 674) 移至 `saw1` (line 669) 之前 |

### Item 5.3 — 引用标签缺名字缩写区分（2处）

| # | 行号 | 问题 | 建议 |
|---|------|------|------|
| 28 | **548, 566** | Chen KL {et~al.} vs Chen and Kang — 同姓不同一作，应加缩写 | → `Chen KL {et~al.}` / `Chen SZ and Kang` |
| 29 | **710, 717** | Zhu vs Zhu {et~al.} — 同姓不同一作 | → `Zhu AD` / `Zhu XE {et~al.}` |

### Items 17–20 — 其他文献项（均通过）

| # | 检查项 | 结果 |
|---|--------|------|
| 17 | 会议名无 `{\em}` | ✅ 通过 — `IEEE MTT-S Int Microwave Symp` 等均未用 `{\em}`，期刊名使用 `{\em}` 正确 |
| 18 | `Parallel`→`Parall.` | ✅ 通过 — 未出现 |
| 19 | 会议序数词上标 | ✅ 通过 — 文献中无需求 |
| 20 | DOI 末尾无句号 | ✅ 通过 — 全部 DOI 末尾无句号 |
| 28 | 多引用按年份升序 | ✅ 通过 — 所有 `\citep{...}` 多引用按年份升序排列 |

---

## 五、其他（共 5 处）

### Item 6 — 章节/后置标题格式（3处）

| # | 行号 | 当前 | 建议 |
|---|------|------|------|
| 30 | **487** | `\section{Conclusion}` | → `\section{Conclusions}`（FITEE 要求复数） |
| 31 | **509** | `\noindent{\textbf{Author contributions}}` | → `\noindent{\textbf{Contributors}}` |
| 32 | **527** | `Declaration on the use of generative AI tools` | → `Declaration on the use of generative AI tool`（规则用单数 `tool`） |

### Item 25 — 正文禁用 `\mbox{}`（1处）

| # | 行号 | 当前 | 建议 |
|---|------|------|------|
| 33 | **7** | `\DeclareRobustCommand{\FPAFB}{\mbox{FPA-FB}}` | → `\DeclareRobustCommand{\FPAFB}{FPA-FB}`。`\mbox` 会阻止换行，违反 6.3 规则 |

### Item 30 — 数字与单位间缺 `~`（系统性）

| # | 典型位置 | 示例 |
|---|---------|------|
| 34 | **241**, **266**, **381** 等多处 | `400 MHz`→`400~MHz`, `3.5 GHz`→`3.5~GHz`, `1.35 dB`→`1.35~dB`, `35 dB`→`35~dB`, `14.5 dB`→`14.5~dB`, `0.25 dB`→`0.25~dB`, `0.01 ns`→`0.01~ns`, `50 $\Omega$`→`50~$\Omega$` 等（系统性，涉数十处） |

### 其他通过项

| 检查项 | 结果 |
|--------|------|
| 7 — 图片 `.eps` 格式 | ✅ 通过 — 所有 `\includegraphics` 无扩展名（LaTeX 默认匹配 `.eps`），无 `.png`/`.pdf`/`.jpg` |
| 8 — `\vspace` 在浮动体前 | ✅ 通过 — 仅 `\thebibliography` 内有 `\vspace{-8pt}`，无浮动体前无效使用 |
| 11 — 禁止列表环境 | ✅ 通过 — 正文无 `enumerate`/`itemize` |
| 21 — `\makeatother` 拼写 | ✅ 通过 |
| 23 — `lmodern` 污染 | ✅ 通过 — 未使用 |
| 24 — `T1{fontenc}` 重复 | ✅ 通过 — 未显式加载 |
| 26 — 浮动体禁用 `[H]` | ✅ 通过 — 全部使用 `[!htb]`/`[!b]`/`[!t]` |
| 27 — 正文变量一致性 | ✅ 通过 — 公式定义的变量在正文中均用 `$...$` |
| 33 — 负数数学模式 | ✅ 通过 — `$-32.2$`, `$-45.1/-47.2$`, `$-$2.8` 等均在 `$` 内 |

---

## 总结：共发现 34 处问题

| 分类 | 问题数 |
|------|--------|
| 一、首页信息 | 0 |
| 二、图表 | 18 |
| 三、公式 | 8 |
| 四、参考文献 | 4 |
| 五、其他 | 5 (含 1 项系统性) |
| **合计** | **34** (含 1 项系统性单位间空格) |

---

请告诉我哪些问题需要修改，我再进行修改操作。
